{% extends 'base.html.twig' %}

{% block title %}Parents{% endblock %}

{% block body %}
    <h1>Parents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parent.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ parent.nom }}</td>
            </tr>
            <tr>
                <th>Postnom</th>
                <td>{{ parent.postnom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ parent.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ parent.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ parent.adresse }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ parent.profession }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ parent.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parents_index') }}">back to list</a>

    <a href="{{ path('app_parents_edit', {'id': parent.id}) }}">edit</a>

    {{ include('parents/_delete_form.html.twig') }}
{% endblock %}
