<form method="post" action="{{ path('app_inscription_eleve_delete', {'id': inscription_eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription_eleve.id) }}">
    <button class="btn">Delete</button>
</form>
