{{ form_start(form) }}{% include 'crud/_form_fields.html.twig' with {form: form} %}<div class="text-end mt-4"><button class="btn btn-primary"><i class="ti ti-device-floppy me-1"></i>{{ button_label|default('Enregistrer') }}</button></div>{{ form_end(form) }}